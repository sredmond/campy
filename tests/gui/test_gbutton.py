"""Tests for the :class:`campy.gui.ginteractors.GButton` class."""
from campy.gui.ginteractors import GButton

from campy.graphics.gwindow import GWindow

# window = GWindow()
# button = GButton("Hello!")
# button.add_click_handler(lambda event: print(event.__dict__))
# window.add(button)
